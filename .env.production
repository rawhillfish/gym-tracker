# Loaded automatically by CRA during `npm run build` (NODE_ENV=production)
# Points to the self-hosted production backend via Cloudflare Tunnel
REACT_APP_API_URL=https://api.jnp-home.com
GENERATE_SOURCEMAP=false
